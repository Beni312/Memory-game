const initialState = {
  cards: [],
  firstGuess: {},
  deckSize: 0,
  tries: 0
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state
    }
  }
};

export default reducer;
